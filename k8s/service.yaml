apiVersion: v1
kind: Service
metadata:
  name: gitops-app
  labels:
    app: gitops-app
spec:
  type: NodePort
  selector:
    app: gitops-app
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
